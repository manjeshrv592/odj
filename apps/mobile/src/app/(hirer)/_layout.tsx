import { Stack } from "expo-router";

/** Approved-hirer area (browse professions → search for a worker). */
export default function HirerLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
