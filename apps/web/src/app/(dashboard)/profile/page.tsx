import { AdminProfile } from "@/components/admin-profile";

/** Admin profile page — edit name/phone/avatar (no OTP) and change email (OTP). */
export default function ProfilePage() {
  return <AdminProfile />;
}
