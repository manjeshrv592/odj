import { PortalUsers } from "@/components/portal-users";

/** Admin "Portal users" route. The layout already enforced admin access. */
export default function PortalUsersPage() {
  return <PortalUsers />;
}
