import { CatalogOverview } from "@/components/catalog-overview";

/** Catalog landing route. The dashboard layout already enforced admin access. */
export default function CatalogPage() {
  return <CatalogOverview />;
}
