# Hoist nothing by default so packages declare their own deps (stricter, fewer phantom deps).
# Expo / React Native need a flatter layout to resolve native modules correctly.
node-linker=hoisted
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
