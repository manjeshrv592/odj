# NOTE: pnpm 10+ no longer reads pnpm-specific settings (node-linker, hoist,
# peer-dependency flags) from `.npmrc`. They live in `pnpm-workspace.yaml`
# (nodeLinker: hoisted, strictPeerDependencies, autoInstallPeers). Keep this file
# only for npm-registry/auth-style settings if ever needed.
